# Face recognition pipeline dependencies
# Tested on Python 3.11+ (laptop dev) and Python 3.13 (Pi 5 Bookworm)

# Core CV
opencv-python>=4.8.0
numpy>=1.24.0,<2.0.0
scipy>=1.10.0

# Face detection + recognition
insightface>=0.7.3
onnxruntime>=1.16.0   # CPU inference — installs onnxruntime-gpu instead if CUDA available

# Landmarks
mediapipe>=0.10.0

# Daemon
flask>=3.0.0
requests>=2.31.0

# Calibration tools
scikit-learn>=1.3.0

# Pi-only (uncomment when deploying to Raspberry Pi)
# picamera2>=0.3.17
