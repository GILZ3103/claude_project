import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartnightmarket.app',
  appName: 'Smart Night Market',
  webDir: 'dist'
};

export default config;
